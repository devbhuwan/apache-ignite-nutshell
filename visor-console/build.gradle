apply plugin: ApacheIgniteVisorDependencyPlugin
apply plugin: 'gradle-one-jar'

dependencies {
    compile project(":common-test")
}

task fullJar(type: OneJar) {
    mainClass = 'ApacheIgniteVisorConsole'
    archiveName = 'full-' + jar.archiveName
}
